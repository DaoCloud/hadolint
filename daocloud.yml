version: 2.0

build:
  lite_image:
    compile:
      build_dir: /
      dockerfile_path: Dockerfile
      cache: true
    extract:
      - /root/.local/bin/hadolint
    package:
      dockerfile_path: Dockerfile.server
      build_dir: /
      cache: true
